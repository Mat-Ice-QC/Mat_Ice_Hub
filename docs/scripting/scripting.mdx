---
pagination_next: null
pagination_prev: null
---

# Scripting

This section contains a collection of scripts I have developed. Unless specified otherwise, all scripts are licensed under the AGPL v3.


import DocCardList from '@theme/DocCardList'; 
import { useCurrentSidebarCategory } from '@docusaurus/plugin-content-docs/client';

export const DocList = () => {
  const category = useCurrentSidebarCategory();
  return (
    <DocCardList items={category.items.filter(item => item.label !== 'Scripting')} />
  );
};

<DocList />
